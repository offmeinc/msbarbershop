--- src/components/manager/DashboardScreen.tsx
+++ src/components/manager/DashboardScreen.tsx
@@ -1016,6 +1016,33 @@
             </button>
           </div>
+
+          {/* Quick Horizontal Date Selector */}
+          <div className="mt-4 flex gap-2 overflow-x-auto no-scrollbar pb-2 px-1 scroll-smooth" style={{ scrollSnapType: 'x mandatory' }}>
+            {Array.from({ length: 14 }).map((_, i) => {
+              const d = addDays(new Date(), i - 3);
+              const isSelected = isSameDay(d, currentDate);
+              const isToday = isSameDay(d, new Date());
+              return (
+                <button
+                  key={i}
+                  onClick={() => setCurrentDate(d)}
+                  style={{ scrollSnapAlign: 'center' }}
+                  className={`flex flex-col items-center justify-center min-w-[60px] h-16 rounded-2xl transition-all duration-300 border ${
+                    isSelected
+                      ? 'bg-amber-500 border-amber-500 text-black shadow-lg shadow-amber-500/20'
+                      : isToday
+                      ? 'bg-white/10 border-amber-500/50 text-amber-500'
+                      : 'bg-white/5 border-white/5 text-neutral-400 hover:text-white hover:bg-white/10 hover:border-white/10'
+                  }`}
+                >
+                  <span className={`text-[10px] font-black uppercase tracking-widest mb-1 ${isSelected ? 'text-black/70' : isToday ? 'text-amber-500/70' : 'text-neutral-500'}`}>
+                    {format(d, 'eee', { locale: ptBR })}
+                  </span>
+                  <span className="text-lg font-black leading-none">{format(d, 'dd')}</span>
+                </button>
+              );
+            })}
+          </div>
         </div>
       )}
 
